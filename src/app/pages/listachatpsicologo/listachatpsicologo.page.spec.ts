import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ListachatpsicologoPage } from './listachatpsicologo.page';

describe('ListachatpsicologoPage', () => {
  let component: ListachatpsicologoPage;
  let fixture: ComponentFixture<ListachatpsicologoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ListachatpsicologoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
